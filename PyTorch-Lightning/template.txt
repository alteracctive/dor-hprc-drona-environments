#!/bin/bash
#SBATCH --job-name=[JOBNAME]
#SBATCH --time=[TIME] --mem=[MEM]
#SBATCH --ntasks=[TASKS] --nodes=[NODES] --cpus-per-task=[CPUS]
#SBATCH --output=out.%j --error=error.%j
[PARTITION]
[EXTRA]

module purge
module load WebProxy

[setupEnv]

cd [flocation]
[tbLaunch]

echo "=== DRONA DEBUG START ==="
echo "Date: \$(date)"
echo "Hostname: \$(hostname)"
echo "Python Command: \$(which python3)"
python3 --version
echo "Active Modules:"
module list 2>&1
echo "=== DRONA DEBUG END ==="

python3 train.py
